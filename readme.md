1. Install dependencies
   npm install

2. Start dev server
   npm run dev

3. Open http://localhost:5173

Login: admin@example.com / password

Notes:
- This boilerplate uses a mock auth-token in localStorage. Replace fakeAuthApi in Login.jsx with real API calls.
- Tailwind is ready. Edit tailwind.config.cjs to add your theme or components.